import { KakimonoConfig } from './type';

export const defaultValue: KakimonoConfig = {
  windowSize: {
    width: 1280,
    height: 720,
    columns: {
      markdownAreaRatio: {
        editor: 50,
        preview: 50,
      },
    },
  },
};
